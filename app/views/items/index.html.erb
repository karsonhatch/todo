<h1>Lists</h1>
<p><%= link_to 'New Item', new_item_path %>
<% @items.each do |item| %>
	<h2><%= item.status %></h2>
	<p><%= item.description %></p>
    <%= link_to 'Show Item', item_path(item) %>
<% end %>
